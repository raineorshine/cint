# Deployment Steps
Add tests
Update README
Update HISTORY
Add and commit all changes
Bump version number in package.json
gulp
git add -A
git commit -m "vX.X.X"
git tag vX.X.X
git push && git push --tags
npm publish